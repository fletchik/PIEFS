# Spotify Songs — binary classification: mode (0=minor, 1=major)
# Audio features: acousticness, danceability, energy, tempo, valence, ... (12 total)
name: spotify
root: data/spotify
task: binary
train_fraction: 0.7
val_fraction: 0.1
standardize: true
input_dim: 12    # 12 audio features (mode excluded — it is the label)
num_classes: 2
