================================================================================
OPENREVIEW SUBMISSION - FINAL VERSION
AI4Physics Workshop @ ICML 2026
================================================================================

TL;DR:
================================================================================
Physics-informed eigenfunction learning with learnable metric A(x) achieves competitive accuracy while discovering geometrically meaningful features grounded in modified Dirichlet energy.


ABSTRACT:
================================================================================
Spectral methods are widely used in machine learning for feature engineering and representation learning. Typically, such approaches rely on defining a kernel or an integral/differential operator whose eigenfunctions are then used to construct data-driven features. In practice, however, the performance of spectral methods is highly sensitive to the choice of kernel, feature scaling, and associated hyperparameters, often requiring substantial manual tuning.

In this work, we propose an alternative spectral framework that alleviates these limitations. Our approach is based on eigenfunctions of a modified Dirichlet energy functional, defined as the expected squared norm of the gradient of a function. The key modification consists in introducing a learnable linear transformation of the gradient within the Dirichlet energy, allowing the spectral structure to be adapted directly from data.

This formulation provides several important benefits. First, it relaxes the classical manifold hypothesis by interpolating between differential-geometric operators, such as the Laplace–Beltrami operator, and purely statistical objects defined by energy spectra. Second, it significantly reduces the need for manual hyperparameter tuning and feature scaling, as the relevant transformations are learned automatically. Finally, the proposed method yields compact and expressive data representations that are naturally aligned with downstream tasks, such as classification.

We evaluate PIEFS on six datasets spanning two synthetic benchmarks (Two Moons, Circles), one tabular classification task (HTRU2), and three image-based problems (MNIST, CIFAR-10 with pretrained ResNet-18 embeddings). On MNIST, the strongest variant reaches 94.53% validation accuracy; on CIFAR-10 embeddings it reaches 85.50% validation accuracy. The rotation-scaling variant applies $A(x) = \Lambda(x) U(x)$ to gradients as $v \mapsto \Lambda(x)(U(x)v)$ so both factors receive non-trivial gradients. NeuralEF reports 82.52% MNIST test accuracy at K=16; our MNIST numbers are validation accuracy and therefore not directly comparable, but the gap motivates further aligned evaluation. Training traces and eigenfunction plots illustrate convergence and geometry-sensitive coordinates.


KEYWORDS:
================================================================================
Physics-informed neural networks; Spectral methods; Eigenfunction approximation; Interpretable feature learning; Differential geometry


SUBMISSION METADATA:
================================================================================
Title: Physics-Informed Eigenfunction Features with Learnable Scaling (PIEFS)

Venue: AI4Physics Workshop @ ICML 2026

Paper: 9 pages, PDF available

Code: [GitHub link to be added after repository creation]

Main Innovation: 
- Learnable metric A(x) = Λ(x)·U(ω(x)) that adapts spectral structure from data
- Modified Dirichlet energy loss instead of fixed kernels
- Sequential eigenfunction learning with stop-gradient mechanisms
- Gram orthogonality constraints with dynamic weight scheduling

Experimental Results:
- Two Moons: 100% accuracy (perfect crescent geometry)
- Circles: 83.6% accuracy
- HTRU2: 97.7% accuracy
- MNIST: 94.53% ± 0.33% validation accuracy (K=16)
- CIFAR-10: 85.50% ± 0.53% on ResNet-18 embeddings

Reproducibility:
- Full codebase available (src/, scripts/, configs/)
- Single-run command: python train.py --dataset mnist --variant off --k 16 --steps 60000 --seed 42
- Requirements: requirements_github.txt
- Expected GPU time: ~5-10 minutes per run (MNIST)
